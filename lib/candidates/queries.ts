/**
 * CCE validation fixture — candidate query layer (not wired into runtime).
 */
export async function listCandidates(): Promise<unknown[]> {
  // Documentación: consultas de candidatos para validación CCE (comments).
  return [];
}
